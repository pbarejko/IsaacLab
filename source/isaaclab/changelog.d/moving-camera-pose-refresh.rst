Fixed
^^^^^

* Documented the need for ``CameraCfg.update_latest_camera_pose=True`` on moving cameras when the renderer
  receives explicit camera poses.
* Clarified that camera render specifications can describe logical instances whose clones are absent
  from the authored USD stage.
